{
  "type": "module",
  "scripts": {
    "test": "jest --verbose",
    "test:coverage": "jest --coverage",
    "lint": "eslint . --ext .js; stylelint '**/*.css'",
    "lint:fix": "eslint . --ext .js --fix; stylelint '**/*.css' --fix",
    "start": "rm -rf dist .parcel-cache && parcel watch manifest.json --host localhost --config @parcel/config-webextension",
    "build": "parcel build manifest.json --config @parcel/config-webextension"
  },
  "license": "GPL-2.0-or-later",
  "dependencies": {
    "tesseract.js": "^5.0.3"
  },
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.22.11",
    "@parcel/config-webextension": "^2.9.1",
    "eslint": "^8.48.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.28.1",
    "eslint-plugin-jest": "^27.4.2",
    "jest": "^29.6.4",
    "jest-environment-jsdom": "^29.6.4",
    "parcel": "^2.9.1",
    "process": "^0.11.10",
    "stylelint": "^15.11.0",
    "stylelint-config-standard": "^34.0.0"
  }
}
